import { Routes } from '@angular/router';
import { LocationPageComponent } from '../pages/locations-page/locations-page.component';

export const SS_LOCATION_ROUTES: Routes = [
  {
    path: '',
    component: LocationPageComponent,
  }
];
